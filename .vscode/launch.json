{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node-terminal",
            "name": "Skript ausführen: start",
            "request": "launch",
            "command": "npm run start",
            "cwd": "${workspaceFolder}"
        },
        {
            "type": "node-terminal",
            "name": "Skript ausführen: build",
            "request": "launch",
            "command": "npm run build",
            "cwd": "${workspaceFolder}"
        }
    ]
}